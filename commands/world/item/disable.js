exports.subcommands = ["d","disable"];
exports.run = async (client, message, args, level, worldname) => {
    // Parameters
	let guildId = message.guild.id;
    // Process
    // Respond
    message.channel.send(`Command valid; Code under construction, kupo. Check back later~`,{code: "asciidoc"});
}
